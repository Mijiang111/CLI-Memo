# Architecture Principles

- Source-of-truth project state lives in local files and structured runtime state.
- The right panel starts with a human brief; deeper ledgers stay available on demand.
- Grep-first retrieval is the default index for memory and architecture context.
- Every completion claim should link to command, file, or review evidence.
