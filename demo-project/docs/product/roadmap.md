# Roadmap

## Now

- Make the terminal UI readable for humans while preserving AI-readable continuity.
- Keep the CLI agent startup path working inside the browser terminal.
- Use summary-first takeover, token budgets, refs, and grep-first retrieval.

## Next

- Stabilize dev-server websocket reconnect behavior.
- Add provider-neutral CLI handoff adapters for Codex, Claude, Kimi, and other agents.
- Improve retrieval ranking with source refs, snippets, and targeted context reads.
