# Quality Policy

- Every completed goal needs acceptance criteria evidence.
- Required gates must pass or be explicitly waived with a reason.
- Test output, command output, review links, and commits can be evidence.
- Skipped checks must be visible in the completion audit.
